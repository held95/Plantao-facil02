# ============================================
# AUTHENTICATION (NextAuth.js)
# ============================================
# Base URL for the application (used for callbacks and links)
NEXTAUTH_URL=http://localhost:3000

# Secret key for JWT encryption (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# ============================================
# AWS CREDENTIALS (Shared by SES and SNS)
# ============================================
# AWS Region where your services are hosted
AWS_REGION=us-east-1

# AWS Access Key ID (from IAM user or role)
AWS_ACCESS_KEY_ID=your_aws_access_key_id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# ============================================
# EMAIL NOTIFICATIONS (AWS SES)
# ============================================
# Enable/disable email notifications
ENABLE_EMAIL_NOTIFICATIONS=true

# Email sender address (must be verified in AWS SES)
AWS_SES_FROM_EMAIL=noreply@plantaofacil.com

# Reply-to email address
AWS_SES_REPLY_TO=suporte@plantaofacil.com

# ============================================
# SMS NOTIFICATIONS (AWS SNS)
# ============================================
# Enable/disable SMS notifications
ENABLE_SMS_NOTIFICATIONS=true

# Note: AWS SNS uses the same AWS credentials configured above
# SMS pricing: ~$0.06 USD per SMS for Brazil
# No additional configuration needed beyond AWS credentials

# ============================================
# APPLICATION
# ============================================
# Application name (displayed in UI and emails)
NEXT_PUBLIC_APP_NAME=Plantão Fácil

# Application version
NEXT_PUBLIC_APP_VERSION=1.0.0

# API base URL (if using external API Gateway)
NEXT_PUBLIC_API_BASE_URL=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com/prod

# ============================================
# DATABASE (Future)
# ============================================
# Uncomment and configure when database is implemented

# PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/plantao_facil

# MongoDB
# MONGODB_URI=mongodb://localhost:27017/plantao_facil

# DynamoDB (AWS)
# Already configured via AWS credentials above

# ============================================
# DEVELOPMENT ONLY
# ============================================
# Set to "development" for local dev, "production" for deployment
NODE_ENV=development

# ============================================
# SETUP INSTRUCTIONS - AWS
# ============================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Generate NEXTAUTH_SECRET:
#    openssl rand -base64 32
#
# 3. Create AWS IAM User:
#    - Go to AWS Console → IAM → Users → Create User
#    - Enable "Programmatic access"
#    - Attach policies: AmazonSESFullAccess, AmazonSNSFullAccess
#    - Save Access Key ID and Secret Access Key
#
# 4. Configure AWS SES (Email):
#    - Go to AWS Console → SES → Verified identities
#    - Verify your domain or email address
#    - Request production access (to send to any email)
#
# 5. Configure AWS SNS (SMS):
#    - Go to AWS Console → SNS → Text messaging (SMS)
#    - Set up SMS preferences (Transactional)
#    - Request spending limit increase if needed
#    - Test with your phone number
#
# 6. Update all placeholder values with real credentials
#
# ============================================
# AWS PRICING (2024 estimates for Brazil)
# ============================================
#
# SES (Email):
# - First 62,000 emails/month: FREE (when sending from EC2)
# - Additional emails: $0.10 per 1,000 emails
#
# SNS (SMS):
# - Brazil: ~$0.06 USD per SMS
# - Example: 1,000 SMS = $60 USD/month
#
# ============================================
# SECURITY NOTES
# ============================================
#
# - NEVER commit .env.local to git
# - Keep credentials secure and rotate regularly
# - Use environment-specific values for dev/staging/prod
# - Enable 2FA on AWS account
# - Monitor usage and set billing alerts
# - Use least-privilege IAM policies
#
# ============================================
